server:
  port: 8091

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: false

eureka:
  client:
    service-url:
      defaultZone: http://eureka-service:8099/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"